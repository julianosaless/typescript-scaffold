{
  "name": "testme",
  "version": "0.1.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/well1791/typescript-scaffold.ts"
  },
  "scripts": {
    "postinstall": "typings install",
    "lint": "tslint {src,test}/**/*.ts",
    "precompile": "npm run lint",
    "compile": "tsc",
    "pretest": "npm run compile",
    "test": "mocha --recursive",
    "typings": "typings"
  },
  "dependencies": {
    "tslint": "^3.13.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "mocha": "^2.5.3"
  }
}
